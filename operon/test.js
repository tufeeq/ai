const assert=require('assert');const E=require('./engine');const s={monthlyRevenue:850000,cashRunwayDays:43,overdueAR:391000,activeProjects:14,atRiskProjects:4,pipeline:6200000,stalePipeline:1360000,vendorLeakage:186000};const score=E.scoreCompany(s);assert(score>=0&&score<=100);const d=E.detectDecisions(s);assert(d.length===5);assert(d[0].impact>=d[1].impact);const after=E.simulate(s,d.find(x=>x.title.includes('collections')));assert(after.overdueAR<s.overdueAR);console.log('OPERON engine tests: PASS',{score,decisions:d.length,afterAR:after.overdueAR});