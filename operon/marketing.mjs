const form=document.querySelector('#value-form');
function calculate(){const values=Object.fromEntries(new FormData(form));if(!form.checkValidity()){document.querySelector('#value-result').textContent='Enter values within the ranges shown.';return;}const hours=Number(values.staff)*Number(values.hours)*4.33*Number(values.reduction)/100,value=hours*Number(values.cost);document.querySelector('#value-result').innerHTML='<div class="value-result"><div><small>Assumed capacity released / month</small><b>'+hours.toFixed(1)+'h</b></div><div><small>Equivalent capacity value / month</small><b>SAR '+Math.round(value).toLocaleString('en')+'</b></div></div>';}
form.addEventListener('input',calculate);form.addEventListener('submit',e=>e.preventDefault());calculate();
document.querySelector('#pilot-form').addEventListener('submit',e=>{e.preventDefault();const data=Object.fromEntries(new FormData(e.target));const text=['OPERON — PILOT BRIEF','', 'Team: '+data.company,'Workflow: '+data.focus,'Success measure: '+data.success,'','1. Capture the baseline: record sample, outstanding exceptions, current handoff steps, and time to completion.','2. Assign a pilot owner and configure the authorization matrix.','3. Import representative records into a dedicated workspace.','4. Run the decision → assignment → completion → verification loop.','5. Compare observed results with the baseline, including unsuccessful and unresolved cases.','','Readiness: public sandbox uses synthetic data and local browser storage. Deploy the persistent server before using team accounts. Billing and external integrations are not connected.','This brief is a planning document, not a contract, paid order, or promise of results.'].join('\n');const url=URL.createObjectURL(new Blob([text],{type:'text/plain;charset=utf-8'}));const a=document.createElement('a');a.href=url;a.download='operon-pilot-brief.txt';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);document.querySelector('#pilot-status').textContent='Pilot brief prepared for download. Nothing was sent.';});
