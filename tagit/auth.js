const ACCESS_HASH='9e0697be2ca367b16a35274defaafecc8df7166d9e965203e2ee00ee2ae025aa';
const SESSION_KEY='tagit-personal-access-v1';
async function sha256(s){const b=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(s));return [...new Uint8Array(b)].map(x=>x.toString(16).padStart(2,'0')).join('')}
function unlock(){document.documentElement.classList.add('tagit-unlocked');document.getElementById('tagitLock')?.remove();import('./app-v4.js')}
function mount(){if(sessionStorage.getItem(SESSION_KEY)==='ok'){unlock();return}const el=document.createElement('div');el.id='tagitLock';el.className='tagit-lock';el.innerHTML=`<div class="tagit-lock-card"><div class="tagit-lock-logo">TAGit</div><h1>Personal Access</h1><p>هذه الأداة مخصصة للاستخدام الشخصي.</p><form id="tagitLockForm"><input id="tagitPassword" type="password" autocomplete="current-password" placeholder="Password" autofocus><button type="submit">دخول</button><div id="tagitLockError" role="alert"></div></form></div>`;document.body.appendChild(el);document.getElementById('tagitLockForm').addEventListener('submit',async e=>{e.preventDefault();const v=document.getElementById('tagitPassword').value||'';const h=await sha256(v);if(h===ACCESS_HASH){sessionStorage.setItem(SESSION_KEY,'ok');unlock()}else{document.getElementById('tagitLockError').textContent='كلمة المرور غير صحيحة';document.getElementById('tagitPassword').select()}})}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',mount);else mount();
