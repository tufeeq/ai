name: TAGit v2.5 Hierarchical Discovery Lab

on:
  push:
    paths:
      - 'tagit/discovery-lab-v25.py'
      - 'tagit/discovery-lab-v24.py'
      - '.github/workflows/tagit-v25-hierarchical-discovery.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          filter: blob:none
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: python -m pip install --disable-pip-version-check numpy scikit-learn
      - name: Syntax check
        run: python -m py_compile tagit/discovery-lab-v25.py
      - name: Run leak-free hierarchical walk-forward
        run: python tagit/discovery-lab-v25.py
      - name: Commit report
        run: |
          git config user.name tagit-v25-research-bot
          git config user.email actions@users.noreply.github.com
          git add tag/data/tagit-v25-hierarchical-discovery.json
          git diff --cached --quiet && exit 0
          git commit -m 'data: add TAGit v2.5 hierarchical discovery results'
          for i in 1 2 3; do
            git pull --rebase --autostash origin main && git push origin HEAD:main && exit 0
            sleep $((i*4))
          done
          exit 1
