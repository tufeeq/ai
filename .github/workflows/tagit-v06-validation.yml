name: TAGit v0.6 Precursor Validation

on:
  push:
    paths:
      - 'tagit/backtest-v06.mjs'
      - 'tagit/core.js'
      - '.github/workflows/tagit-v06-validation.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Syntax check
        run: |
          node --check tagit/core.js
          node --check tagit/backtest-v06.mjs
      - name: Run precursor discovery and promotion walk-forward
        run: node tagit/backtest-v06.mjs
      - name: Commit validation report
        run: |
          git config user.name tagit-v06-validation-bot
          git config user.email actions@users.noreply.github.com
          git add tag/data/tagit-v06-walkforward.json
          git diff --cached --quiet && exit 0
          git commit -m 'data: update TAGit v0.6 precursor validation'
          for i in 1 2 3; do git pull --rebase origin main && git push origin HEAD:main && exit 0; sleep $((i*4)); done
          exit 1
