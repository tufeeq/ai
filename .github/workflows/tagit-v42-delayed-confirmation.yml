name: TAGit v4.2 Delayed Confirmation Frozen Validation

on:
  workflow_dispatch:
  push:
    paths:
      - 'tagit/delayed-confirmation-v42-backtest.py'
      - 'tagit/event-conviction-v41-backtest.py'
      - 'tag/data/tagit-v39-frozen-groundtruth.json'
      - '.github/workflows/tagit-v42-delayed-confirmation.yml'

permissions:
  contents: write

concurrency:
  group: tagit-v42-delayed-confirmation-main
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 2
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Sync main
        run: git pull --rebase origin main
      - name: Install research dependencies
        run: python -m pip install --disable-pip-version-check numpy scikit-learn
      - name: Compile v4.2 backtest
        run: python -m py_compile tagit/event-conviction-v41-backtest.py tagit/delayed-confirmation-v42-backtest.py
      - name: Run frozen v4.2 validation
        run: python tagit/delayed-confirmation-v42-backtest.py
      - name: Verify frozen contract
        run: |
          python - <<'PY'
          import json,pathlib
          p=json.loads(pathlib.Path('tag/data/tagit-v42-delayed-confirmation-backtest.json').read_text())
          assert p['datasetSha256']=='19277cfddc8dbeaebee73ce70a6e6fad57213caae87959da7f3e5e859a88773b'
          assert p['policy']=='RESEARCH_ONLY_NO_CHAMPION_OVERRIDE'
          assert p['promotionRule']['autoPromotion'] is False
          print(json.dumps({'verdict':p['verdict'],'selectedPolicies':p['selectedPolicies'],'holdout':p['holdout']},indent=2))
          PY
      - name: Commit derived reports only
        run: |
          git config user.name tagit-research-bot
          git config user.email actions@users.noreply.github.com
          git add tag/data/tagit-v41-event-conviction-backtest.json tag/data/tagit-v42-delayed-confirmation-backtest.json
          git diff --cached --quiet && exit 0
          git commit -m 'data: refresh TAGit v4.2 delayed confirmation validation'
          for i in 1 2 3; do
            git pull --rebase --autostash origin main && git push origin HEAD:main && exit 0
            sleep $((i*6))
          done
          exit 1
