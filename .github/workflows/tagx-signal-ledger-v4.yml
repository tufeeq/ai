name: TAGX Signal Ledger v4 (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Retired: the authoritative TAGX ledger is now written by tagx-authoritative-ledger.yml."
          echo "This workflow remains manual-only to preserve audit history and avoid competing writers."
