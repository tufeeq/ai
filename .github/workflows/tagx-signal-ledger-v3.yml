name: TAGX Signal Ledger v3 (superseded)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Superseded by integrated Signal Ledger v4 inside tag500-discovery-universe.yml. Manual diagnostic only; no writes."
