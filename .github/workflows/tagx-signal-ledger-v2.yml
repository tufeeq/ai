name: TAGX Signal Ledger v2 (legacy)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Superseded by TAGX Signal Ledger v3. Manual-only legacy workflow; no ledger writes."
